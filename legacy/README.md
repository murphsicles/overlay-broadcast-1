# legacy/

REQ-GOV-070: any pre-existing foundation tree present at build start is moved here
verbatim and retained; nothing is deleted or overwritten; git history is never
rewritten or force-pushed.

At the start of this build there was no pre-existing foundation tree for this project
to archive, so this directory is a placeholder establishing the preservation policy.
Prior specification versions (v1, v2) are preserved alongside v3 and are not deleted.
